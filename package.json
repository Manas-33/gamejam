{
  "name": "protocol-unmask",
  "version": "1.0.0",
  "private": true,
  "description": "Protocol: UNMASK - Multiplayer social deduction party game",
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "postinstall": "npm run install:all",
    "build": "npm run build --prefix client",
    "start": "node server/src/index.js & npx --prefix client vite preview --host 0.0.0.0 --port 5000",
    "dev": "npm run dev --prefix server & npm run dev --prefix client"
  },
  "engines": {
    "node": ">=18"
  }
}
