## Packages
framer-motion | For smooth animations and transitions between game phases
lucide-react | Icon library for the spy/heist aesthetic
react-confetti | For the victory screen celebration

## Notes
WebSocket connection should handle reconnection logic automatically if possible, or at least alert the user.
The drawing canvas for the player identity needs to support touch events for mobile users.
Host view is intended for large screens (projectors), Player view for mobile.
